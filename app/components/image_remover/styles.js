const styles = StyleSheet.create({
  image_remover: {
    width: 55,
    height: 55,
    justifyContent: "center",
  },

  menu_item: {
    backgroundColor: "white",
    fontSize: 18,
    padding: 20,
  }
});

export default styles;
