const styles = StyleSheet.create({
  image_remover: {
    width: 50,
    height: 50,
    justifyContent: "center",
  },

  menu_item: {
    backgroundColor: "white",
    fontSize: 18,
    padding: 20,
  }
});

export default styles;
