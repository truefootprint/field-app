const styles = StyleSheet.create({
  wrapper: {
    paddingBottom: 20,
    borderBottomWidth: 3,
    borderBottomColor: "#eee",
  },

  last_child: {
    marginBottom: 0,
  }
});

export default styles;
