const styles = StyleSheet.create({
  downloader: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
    minHeight: 200,
    backgroundColor: "white",
  },

  retry: {
    marginTop: 10,
  },
});

export default styles;
