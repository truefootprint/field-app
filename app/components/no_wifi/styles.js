const styles = StyleSheet.create({
  no_wifi: {
    flex: 1,
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center",
  },

  arrow: {
    marginLeft: "8%",
    marginRight: "8%",
    color: palette.green.primary,
  }
});

export default styles;
