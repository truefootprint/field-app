#!/bin/bash

if [ "$#" -ne 1 ]; then
  echo "Usage: ./bin/deploy /path/to/expo-credentials.json"
  exit 1
fi

./bin/clean            && \
./bin/switch_env prod  &&
./bin/export           && \
./bin/build $@         && \
./bin/push_gh_pages    && \
./bin/upload $@
